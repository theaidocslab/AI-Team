# Captured assets

No live crawl was possible (demo.theaidocslab.com is blocked by this session's
network egress policy). Assets below were extracted by hand from the local
mirror at `/home/user/AI-Team/demo/live-demo-coach.html`.

- `assets/logo-icon.svg` — the site's real favicon/mark: a gold (`#C9A84C`)
  lightning-bolt polygon on a black (`#0A0A0A`) rounded-square field, 32x32
  viewBox. This is the brand's only graphic asset; there are no product
  screenshots or photos on the page — it's a fully typographic/CSS design.
  Use this as the logo sting / bug throughout the video.
- No other image assets exist on the source page. The "lead capture form"
  visual is CSS-styled HTML (inputs, a select, textareas) — the video's form
  mockup should be built from the real field list in `visible-text.txt`
  (name, email, website/handle, niche, ideal client, offer, biggest result,
  lead-gen-problem dropdown, booking link), styled with the captured brand
  tokens, not a screenshot.
